# Avoid running heroku update on each build
dependencies:
  pre:
    - heroku update
  cache_directories:
    - ~/.heroku

test:
  override:
    - script/pipeline.sh addie_tests 'bundle exec rake'

deployment:
  production:
    branch: master
    commands:
      - script/pipeline.sh addie_deploy_production "script/deploy.sh"
